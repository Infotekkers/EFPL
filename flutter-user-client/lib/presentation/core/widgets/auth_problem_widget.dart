import 'package:flutter/material.dart';

class AuthProblemWidget extends StatelessWidget {
  const AuthProblemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/images/403.png",
    );
  }
}
